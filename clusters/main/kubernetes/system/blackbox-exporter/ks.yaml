apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: blackbox-exporter
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/system/blackbox-exporter/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

