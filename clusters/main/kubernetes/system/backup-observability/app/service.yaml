---
apiVersion: v1
kind: Service
metadata:
  name: backup-observability
  namespace: backup-observability
  labels:
    app.kubernetes.io/name: backup-observability
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backup-observability
  ports:
    - name: http
      port: 8080
      targetPort: http
