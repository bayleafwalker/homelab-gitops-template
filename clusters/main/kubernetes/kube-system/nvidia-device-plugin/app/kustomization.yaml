---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - daemonset.yaml
  - rbac.yaml

configMapGenerator:
  - name: nvidia-device-plugin-config
    namespace: kube-system
    files:
      - any=nvidia-device-plugin-config.any
