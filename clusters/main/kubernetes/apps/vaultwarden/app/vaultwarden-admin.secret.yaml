# Sample only — generate your own random token (e.g. `openssl rand -base64 48`)
# and re-encrypt with sops/age before committing.
apiVersion: v1
kind: Secret
metadata:
    name: vaultwarden-admin
    namespace: vaultwarden
type: Opaque
stringData:
    ADMIN_TOKEN: REPLACE_WITH_RANDOM_TOKEN
